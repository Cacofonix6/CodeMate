
public class Foo {
	
	// 1
	public void cyclomaticFunc()  {
	
		int b = 2;
		int c = 3;
		int result;
		
		// 2
		result = (b > c) ? b : c;
		// 3
		result = (b > c) ? b : c;
		// 4
		result = (b > c) ? b : c;
		// 5
		result = (b > c) ? b : c;
		// 6
		result = (b > c) ? b : c;
		// 7
		result = (b > c) ? b : c;
		// 8
		result = (b > c) ? b : c;
		// 9
		result = (b > c) ? b : c;
		// 10
		result = (b > c) ? b : c;
		// 11
		result = (b > c) ? b : c;
		// 12
		result = (b > c) ? b : c;
		// 13
		result = (b > c) ? b : c;
		// 14
		result = (b > c) ? b : c;
		// 15
		result = (b > c) ? b : c;
		// 16
		result = (b > c) ? b : c;
		// 17
		result = (b > c) ? b : c;
		// 18
		result = (b > c) ? b : c;
		// 19
		result = (b > c) ? b : c;
		// 20
		result = (b > c) ? b : c;
		// 21
		result = (b > c) ? b : c;
    }
	
	// 1
	public void controlFunc()  {
	
    }
}